{
  "homepage": "http://leaortiz.github.io/leave-wizard-organization-employees",
  "name": "leave-wizard-organization-employees",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.11.4",
    "@testing-library/react": "^11.1.0",
    "@testing-library/user-event": "^12.1.10",
    "bootstrap": "^3.3.7",
    "dotenv": "^6.1.0",
    "env-cmd": "^8.0.2",
    "moment": "^2.29.1",
    "node": "^15.8.0",
    "react": "^16.12.0",
    "react-bootstrap": "^0.33.1",
    "react-dom": "^16.12.0",
    "react-icons": "^4.2.0",
    "react-scripts": "4.0.2",
    "source-sans": "^3.28.0",
    "web-vitals": "^1.0.1"
  },
  "scripts": {
    "start": "env-cmd .env react-scripts start",
    "build": "react-scripts build",
    "build:local": "env-cmd .env npm run build",
    "build:uat": "env-cmd .env.uat npm run build",
    "build:prd": "env-cmd .env.prd npm run build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
